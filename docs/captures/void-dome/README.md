# void sky dome — before / after

The same four void shots either side of 2026-07-26's change, 1600x900, 90
pumped frames, same machine and same session.

- `before/` — the baked-impostor far band, no dome. This is the build Ethan
  looked at when he said *"honestly the random shapes in the background is very
  weak hoping the image method will improve it."*
- `after/` — `public/sky/void-dome.png` wired in as the sky dome, and the
  impostor far band deleted.

`summit` and `plunge` carry the most background and are the two to read first.
Numbers and the argument are in `docs/changelog.md`.
